# WEAV AI 백엔드 환경변수 설정
# 이 파일을 .env로 복사하여 실제 값들을 설정하세요

# ===== Django 기본 설정 =====
# 보안을 위한 시크릿 키 (랜덤 문자열로 변경 필수)
SECRET_KEY=your-super-secret-key-change-this-immediately

# 디버그 모드 (프로덕션에서는 False)
DEBUG=False

# 허용 호스트 (쉼표로 구분)
ALLOWED_HOSTS=localhost,127.0.0.1,weavai.ai,api.weavai.ai

# ===== 데이터베이스 (PostgreSQL) =====
POSTGRES_DB=weavai
POSTGRES_USER=weavai_user
POSTGRES_PASSWORD=your-secure-database-password

# ===== Redis (Celery 브로커/결과 백엔드) =====
# Docker Compose에서는 자동으로 설정되므로 변경 불필요
# CELERY_BROKER_URL=redis://redis:6379/0
# CELERY_RESULT_BACKEND=redis://redis:6379/0

# ===== MinIO 스토리지 설정 =====
# MinIO 데이터 저장 경로 (외장하드 경로)
MINIO_DATA_DIR=/Volumes/WEAVAI_2T/minio-data

# MinIO 액세스 키 (짧고 기억하기 쉬운 이름)
MINIO_ACCESS_KEY=weavai_admin

# MinIO 시크릿 키 (매우 긴 랜덤 문자열로 변경 필수)
MINIO_SECRET_KEY=your-very-long-random-secret-key-at-least-32-characters

# MinIO 버킷 이름
MINIO_BUCKET=weavai-files

# ===== AI 서비스 API 키 =====
# FAL.ai API 키 (https://fal.ai에서 발급받기)
FAL_KEY=your-fal-ai-api-key

# ===== 추가 설정 (선택사항) =====
# Cloudflare Tunnel 설정 시 필요할 수 있음
# CLOUDFLARED_CONFIG=/Users/sondongbin/.cloudflared/config.yml
# CLOUDFLARED_TUNNEL_NAME=weavai
# CLOUDFLARED_BIN=/opt/homebrew/bin/cloudflared
ENABLE_EXTERNAL_ACCESS=false

# ===== 백업/모니터링 (선택사항) =====
# 백업 대상 루트 (외장하드)
BACKUP_ROOT=/Volumes/WEAVAI_2T/weavai-backups

# 헬스체크 모니터링
HEALTHCHECK_URLS=http://localhost:8080/healthz,http://localhost:8080/api/v1/health/
# MONITOR_WEBHOOK_URL=https://hooks.slack.com/services/XXXX/XXXX/XXXX
# MONITOR_WEBHOOK_FORMAT=slack  # 또는 discord
# HEALTHCHECK_TIMEOUT_SECONDS=5

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 파일 업로드 크기 제한 (MB)
MAX_UPLOAD_SIZE_MB=100

# Presigned URL 만료 시간 (초)
PRESIGNED_URL_EXPIRATION=3600

# ===== AI 서비스 API 키들 =====
# OpenAI (텍스트/이미지/비디오 생성)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_TEXT_MODEL=gpt-4o-mini

# Google Gemini (텍스트/이미지 생성)  
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_TEXT_MODEL=gemini-1.5-flash

# 기본 AI 제공자 (openai 또는 gemini)
AI_PROVIDER_DEFAULT=openai

# ===== AI 사용 제한 =====
MAX_TEXT_CHARS=8000
MAX_OUTPUT_TOKENS=1024

# ===== 기타 AI 설정 =====
# FAL.ai (비디오 생성 - 추후 확장)
FAL_KEY=your-fal-key-here
# ===== 멤버십/결제 (기본 비활성) =====
ENFORCE_MEMBERSHIP=false
ENABLE_BILLING=false
ALLOW_BILLING_IN_DEBUG=false
